global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: llm-guard-dlp
    metrics_path: /metrics
    static_configs:
      - targets: ["dlp:8001"]

rule_files:
  - /etc/prometheus/alerts.yml
