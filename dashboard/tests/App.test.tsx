import {render,screen} from '@testing-library/react'; import App from '../src/App'; import {vi} from 'vitest'; vi.mock('../src/api',()=>({getMetrics:vi.fn().mockResolvedValue([])})); vi.mock('../src/websocket',()=>({connectEvents:vi.fn(()=>()=>{})})); test('renders dashboard tabs',()=>{render(<App/>); expect(screen.getByText('Overview')).toBeTruthy(); expect(screen.getByText('Events')).toBeTruthy();});